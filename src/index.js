import React from 'react';
import ReactDOM from 'react-dom';
import VechicleService from './VechicleService';
import reportWebVitals from './reportWebVitals';

ReactDOM.render(
  <React.StrictMode>
    <VechicleService />
  </React.StrictMode>,
  document.getElementById('root')
);

reportWebVitals();
